<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "../mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.cmfv.dao.LogMapper">

    <insert id="insert">
        insert into t_log values (#{id},#{user},#{time},#{resource},#{operation},#{message},#{result})
    </insert>

</mapper>
